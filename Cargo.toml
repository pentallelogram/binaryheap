[package]
name = "binaryheap"
version = "0.1.0"
authors = ["a <a@a.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[profile.release]
lto = "fat"
opt-level = 3
panic = "abort"
